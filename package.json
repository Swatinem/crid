{
  "name": "crid",
  "version": "1.0.0",
  "description": "Crid - The cryptographic ID serializer",
  "main": "dist/crid.cjs.js",
  "module": "dist/crid.esm.js",
  "browser": "dist/crid.umd.js",
  "types": "dist/types/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "nyc": "^12.0.2",
    "rimraf": "^2.6.2",
    "rollup": "^0.60.7",
    "rollup-plugin-node-resolve": "^3.3.0",
    "rollup-plugin-sucrase": "^1.0.0",
    "ts-node": "^6.1.1",
    "typescript": "^2.9.2",
    "zora": "^2.0.1"
  },
  "scripts": {
    "build": "rimraf dist && tsc && rollup -c",
    "test": "nyc --reporter=html --reporter=json --reporter=text-summary node test/index.ts"
  },
  "author": "Arpad Borsos <arpad.borsos@googlemail.com>",
  "license": "LGPL-3.0",
  "nyc": {
    "require": [
      "ts-node/register/transpile-only"
    ],
    "extension": [
      ".ts"
    ]
  }
}
